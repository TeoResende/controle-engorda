# --- Domínios (dev usa *.localhost via Traefik) ---
DOMAIN=localhost
API_HOST=api.localhost
APP_HOST=app.localhost
MINIO_HOST=minio.localhost
TRAEFIK_HTTP_PORT=8081
TRAEFIK_HTTPS_PORT=8443
TRAEFIK_DASHBOARD_PORT=8090
ACME_EMAIL=fnettosgs@gmail.com

# --- Postgres ---
POSTGRES_USER=engorda
POSTGRES_PASSWORD=engorda_dev_senha
POSTGRES_DB=engorda

# --- Backend ---
# UID/GID do host: o backend roda com eles para não criar arquivos root
# no bind mount (migrations geradas pelo Alembic).
UID_HOST=1000
GID_HOST=1000
SECRET_KEY=troque-esta-chave-em-producao
ACCESS_TOKEN_EXPIRE_MINUTES=720
REFRESH_TOKEN_EXPIRE_DAYS=30
LOG_LEVEL=info

# --- MinIO ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin_dev
MINIO_BUCKET=observacoes

# --- Frontend ---
# Precisa ser um endereço que o NAVEGADOR alcance (não "backend:8000").
# Da própria máquina: http://api.localhost:8081
NEXT_PUBLIC_API_URL=http://api.SEU-IP-LOCAL.nip.io:8081
